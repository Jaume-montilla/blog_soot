services:
  backend:
    build: backend/
    environment:
      MYSQL_HOST: db
      MYSQL_USER: testing
      MYSQL_PASSWORD: AlbedoScope123
      MYSQL_DB: blog
      MYSQL_PORT: 3306
    ports:
      - "8080:80"
  web:
    build: .
    ports:
      - "5173:5173"
  db:
    image: mysql:latest
    volumes:
      - "./sql/backup:/docker-entrypoint-initdb.d"
    environment:
      MYSQL_ROOT_PASSWORD: RootJaumePC1.
      MYSQL_DATABASE: blog
    ports:
      - "3308:3306"
